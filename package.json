{
  "name": "pi-agentic-search",
  "version": "0.1.2",
  "type": "module",
  "license": "Apache-2.0",
  "keywords": ["pi-package", "pi-extension", "research", "search", "synthesis"],
  "description": "A pi extension that provides a deep research agent that searches, fetches, and synthesizes web information",
  "files": ["extensions"],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jandrikus/pi-agentic-search.git"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "@earendil-works/pi-ai": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
